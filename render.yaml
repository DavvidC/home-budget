services:
  - type: web
    name: home-budget
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ALLOWED_EMAILS
        sync: false
